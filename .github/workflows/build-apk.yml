name: Build Android APK Only

on:
  workflow_dispatch: # 수동 실행만 가능 (자동 빌드는 build-all.yml 사용)

permissions:
  contents: write  # Release 생성 권한

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
      
      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.1'
          channel: 'stable'
      
      - name: 📦 Install dependencies
        run: flutter pub get
      
      - name: 🔍 Analyze code
        run: flutter analyze --no-fatal-infos --no-fatal-warnings || true
      
      - name: 🏗️ Build APK
        run: flutter build apk --release
      
      - name: 📝 Get version info
        id: version
        run: |
          VERSION=$(grep 'version:' pubspec.yaml | awk '{print $2}' | cut -d'+' -f1)
          BUILD_NUMBER=$(grep 'version:' pubspec.yaml | awk '{print $2}' | cut -d'+' -f2)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "build_number=$BUILD_NUMBER" >> $GITHUB_OUTPUT
          echo "Version: $VERSION+$BUILD_NUMBER"
      
      - name: 📤 Create Release and Upload APK
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.version.outputs.version }}-${{ github.run_number }}
          name: v${{ steps.version.outputs.version }} Build ${{ github.run_number }}
          body: |
            ## 🎉 건양대학교병원 환자 앱 v${{ steps.version.outputs.version }}
            
            ### 📱 다운로드
            아래 APK 파일을 다운로드하여 설치하세요.
            
            ### 🔄 변경사항
            - 최신 코드 반영
            - 자동 빌드 및 배포
            
            ### 📅 빌드 정보
            - 빌드 번호: ${{ github.run_number }}
            - 커밋: ${{ github.sha }}
            - 빌드 일시: ${{ github.event.head_commit.timestamp }}
          files: |
            build/app/outputs/flutter-apk/app-release.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: ✅ Build Success
        run: |
          echo "🎉 APK 빌드 및 릴리스 완료!"
          echo "📦 릴리스 태그: v${{ steps.version.outputs.version }}-${{ github.run_number }}"

